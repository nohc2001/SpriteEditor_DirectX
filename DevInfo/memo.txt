1. 글자 출력 - 핸드폰에 있는 ttfParser 임포트
2. UI만들기
3. SpriteEditor
4. InsideCode in MSVC 어느정도 완성이 필요.

문제점.
(해결)1. 왜인지 대부분이 아예그려지지 않는다. 검은색 뭔 창이 앞을 가리기도 했다.
(그냥 거꾸로 쓰기로 함)2. 렌더링 순서를 어떻게 정하는지 정확히 알 수 없다. > 현재로써는 먼저 렌더링한게 맨 앞에 위치한 매우 이상한 현상을 보인다.
3. 알파블랜딩이 불안하게 된다. 누구는 되고 누구는 안된다. (알아보기)
4. 프리폴리곤 구성하는 알고리즘에 문제가 생겼는지, 점을 옮기다 보면 가끔씩 크래시난다. (도형들이 곂칠때 이런 현상이 나타남. >> 거의 모든 경우에 고침
5. 오른쪽 클릭이 인식이 안되나?

-> 부족한거
InsideCode 
-> 수식 괄호 인식이 불안정 (고침)
-> 변수에 값을 대입할때, 
	값이면 8미만이면 무조건 A = *A 처리. -> 근데 이걸 LA에 넣을 방법이 있음? (연산을 하나 더 만들어라..)
	값이 8 이상이면 무조건 그냥 나뒀다가 -> *LA=*A_N 처리
	PARAM_8 만들자. *LA=*A_8 만들자.
-> 주석처리 ()
-> i += const 명령 만들기
-> extened_Instruction -> 오직 상수 파라미터만 받을때의 명령
exGraphic
-> 디버깅용 선 그리기 / 텍스트 출력
	-> gdbg(pos) -> pos 표시.... 이건 그냥 내부구조상 내장 함수가 있어야 할듯?
	-> 기본 parameter로 isGraphic_DBG 있어야 작동?
-> 원과 선의 교차점을 찾는 함수가 가끔 오류남 nan nan
exGeometry
1. sin, cos, tan ...
EuclideMachine -> IC 넣어지면 파라미터가 자동으로 인식되고, 타입에 따라 조정할 수 있게 하기.

IC 연계 단축기 명령어
A add and set

G global variable (param)
V variable
F function

in variable
C circle
F float
T text
R ray(line)

-> 이름 입력
-> 코드 에디터 창이 나와서 ic 업데이트

InsideCode의 실행방법은 다양하다
하지만 실행에는 3가지 과정이 생기는데,
1. 코드의 텍스트를 모두 읽어들이는 과정
2. 코드의 텍스트를 코드데이터로 변환.
3. 코드데이터들을 컴파일.

실행 과정 많은 부분에서 사용자가 수정을 할 수 있게 해야 한다.
예를 들어 코드의 텍스트 부분에서 1000번째 글자를 0에서 1로 바꾸거나,
3번째 코드 데이터인 반복문을 없애는 것 같은 수정을 할 수 있겠다.
그러기 위해 어떤 함수를 준비해야 하는가?

1. 코드 파일 불러와서 단어 추출하는 함수
2. 데이터의 코드라인 데이터를 불러와 단어 추출하는 함수.
3. 해석하여 코드 데이터를 만드는 함수.
4. 코드데이터를 컴파일하는 함수 > 부분컴파일도 목표에 있지만 아직임.
> 4 번 과정이 진행되는 중에는 ECS로 코드를 돌릴 수 없다.

> 과정이 분해된 만큼 릴리즈도 분해되어야 함. 분리...

// 코드 정리 하자..

-> & 문제
보통 8바이트의 값을 메모리에 저장할때 
그 8바이트의 값이 저장된 주소값을 사용해서 메모리에 저장함.
근데 &를 사용하면 A에 주소값이 저장된채로 있기 때문에
주소값이 저장된 주소가 없음. > 때문에 포인터용 혹은 8바이트용 전달 연산을 만들어야 함.

바꿨는데,
이제까지는 8바이트면 다 주소로 돌려서 문제 없던 것들이 문제 생김
익스텐션 함수 하고 난 후 언제 값을 가져오고 언제 주소를 가져와야 할지 모르겠음..
