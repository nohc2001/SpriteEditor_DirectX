1. 글자 출력 - 핸드폰에 있는 ttfParser 임포트
2. UI만들기
3. SpriteEditor
4. InsideCode in MSVC 어느정도 완성이 필요.

문제점.
(해결)1. 왜인지 대부분이 아예그려지지 않는다. 검은색 뭔 창이 앞을 가리기도 했다.
(그냥 거꾸로 쓰기로 함)2. 렌더링 순서를 어떻게 정하는지 정확히 알 수 없다. > 현재로써는 먼저 렌더링한게 맨 앞에 위치한 매우 이상한 현상을 보인다.
3. 알파블랜딩이 불안하게 된다. 누구는 되고 누구는 안된다. (알아보기)
4. 프리폴리곤 구성하는 알고리즘에 문제가 생겼는지, 점을 옮기다 보면 가끔씩 크래시난다. (도형들이 곂칠때 이런 현상이 나타남. >> 거의 모든 경우에 고침
5. 오른쪽 클릭이 인식이 안되나?

-> 부족한거
InsideCode 
-> 수식 괄호 인식이 불안정
-> 변수에 값을 대입할때, 
	값이면 8미만이면 무조건 A = *A 처리. -> 근데 이걸 LA에 넣을 방법이 있음? (연산을 하나 더 만들어라..)
	값이 8 이상이면 무조건 그냥 나뒀다가 -> *LA=*A_N 처리
	PARAM_8 만들자. *LA=*A_8 만들자.
-> 주석처리
exGraphic
-> 디버깅용 선 그리기 / 텍스트 출력
exGeometry
1. sin, cos, tan ...
